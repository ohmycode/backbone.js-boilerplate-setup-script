clone, double click start_backbone_project.command if you're on a mac or run omc_bin/setup.sh on the CLI.

This will create a new directory called rename_me and remove the script files.

1 minute of workflow saved :)